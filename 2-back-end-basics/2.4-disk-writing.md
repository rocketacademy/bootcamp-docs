# 2.4: Disk Writing

```text
const fs = require('fs')

const content = 'Some content!'

fs.writeFile('test.txt', (content, err) => {
  if (err) {
    console.log(err)
    return
  }else{
    console.log('success!');
    //file written successfully
  }
})
```

